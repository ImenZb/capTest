import { DataLocalPipe } from './data-local.pipe';

describe('DataLocalPipe', () => {
  it('create an instance', () => {
    const pipe = new DataLocalPipe();
    expect(pipe).toBeTruthy();
  });
});
